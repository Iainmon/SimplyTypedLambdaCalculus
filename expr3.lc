
T := (\a.(\b.(a)));
K := T;
S := (\x.(\y.(\z.((x z)(y z)))));
I := (\x.x);
F := S K;

cons := (\h.(\l.(\con.(con h l))));
head := (\l.l (\a.(\b.a)));
tail := (\l.l (\a.(\b.b)));
mylist := (cons x (cons y nil));
pair of (head mylist) (tail mylist);

flip := (\a.(\b.(\c.(a c b))));

(\x.x:Type) (y:Type);

nil := nil; -- Circular reference

nil