

cons := (\h.(\l.(\con.(con h l))));
head := (\l.l (\a.(\b.a)));
tail := (\l.l (\a.(\b.b)));
mylist := (cons x (cons y nil));
pair of (head mylist) (tail mylist);


(\x -> (x:Type)) (y:Type);

nil := nil;

id := (lambda x -> x);

id nil; -- hello

(lambda x -> x);

flip := (lambda a ->
  (lambda b ->
    (lambda c ->
      (a c b)
    )
  ));
  